process.container = 'gcr.io/nextflow-250616/sratools'
docker.enabled = true

process {
  maxRetries = 3
  withName:getAccession {
    cpus = 10
    errorStrategy = { task.exitStatus in [1,3] ? 'ignore' : 'retry' }
  }
}